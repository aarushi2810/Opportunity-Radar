# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sse-starlette>=1.8.2
python-multipart>=0.0.9

# ORM + Database drivers
sqlalchemy>=2.0.30
asyncpg>=0.29.0          # Postgres async driver (production on Render)
aiosqlite>=0.20.0        # SQLite async driver (local dev — no setup required)

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Cache
redis>=5.0.4             # Real Redis client (Render Redis add-on)
fakeredis>=2.21.0        # In-memory Redis stub for local dev

# HTTP
aiohttp>=3.9.5           # NSE/Alpha Vantage live data fetches

# AI
google-generativeai>=0.7.2

# Validation
pydantic>=2.7.0

# Utilities
python-dotenv>=1.0.1
httpx>=0.27.0
